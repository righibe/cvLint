# Copy to .env on the server and fill in. Never commit .env.

# Domain served by Caddy (DNS A/AAAA record must point to this server).
SITE_DOMAIN=cvlint.righi.dev

# Public URL used for canonical links, sitemap and robots (inlined at build time).
NEXT_PUBLIC_SITE_URL=https://cvlint.righi.dev
